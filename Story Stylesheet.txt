}
    
    .progress-step {
        margin: 0 0.5em 1em;
    }
    
    .pronoun-buttons, .suggestion-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .navigation-controls {
        flex-direction: column;
        gap: 1em;
    }
    
    .stat-block {
        grid-template-columns: 1fr auto auto auto;
    }
    
    .stat-block label {
        grid-column: 1 / -1;
        text-align: center;
    }
    
    .character-header {
        flex-direction: column;
    }
}

@media (max-width: 480px) {
    html {
        font-size: 14px;
    }
    
    .game-title {
        font-size: 2.5em;
    }
    
    .game-subtitle {
        font-size: 1.2em;
    }
    
    #passages {
        width: 95%;
        padding: var(--spacing-sm) 0;
    }
    
    .stat-grid {
        grid-template-columns: 1fr;
    }
    
    .character-info-grid, .character-stats-summary {
        grid-template-columns: 1fr;
    }
}

/* SugarCube-specific compatibility styles */
#ui-dialog {
    background-color: var(--dark-secondary);
    border: 2px solid var(--neon-primary);
    border-radius: var(--border-radius);
    box-shadow: 0 0 20px rgba(0, 255, 187, 0.3);
}

#ui-dialog-titlebar {
    background-color: var(--dark-primary);
    border-bottom: 1px solid var(--neon-primary);
}

#ui-dialog-title {
    color: var(--neon-primary);
}

#ui-dialog-close {
    color: var(--neon-secondary);
}

#ui-dialog-body {
    background-color: var(--dark-secondary);
    color: #E0E0E0;
}

.ui-close {
    background-color: var(--neon-primary-dim);
    color: var(--neon-primary);
    border: 1px solid var(--neon-primary);
}

.ui-close:hover {
    background-color: var(--neon-primary);
    color: var(--dark-primary);
}

/* Accessibility and mobile friendly focus/active states */
@media (hover: hover) {
    a:hover, button:hover, .title-button:hover, .glow-button:hover {
        transform: scale(1.05);
    }
}

@media (hover: none) {
    a:active, button:active, .title-button:active, .glow-button:active {
        background-color: var(--neon-primary);
        color: var(--dark-primary);
    }
}

/* Ensure readable focus states even without hover */
a:focus, button:focus, input:focus, select:focus {
    outline: 2px solid var(--neon-secondary);
    outline-offset: 2px;
}

/* Additional custom macro support */
.macro-statBlock {
    margin-bottom: var(--spacing-sm);
}

/* Remove scrollbars for aesthetic reasons but keep functionality */
::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

::-webkit-scrollbar-track {
    background: var(--dark-primary);
}

::-webkit-scrollbar-thumb {
    background: var(--neon-primary-dim);
    border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--neon-primary);
}

/* Ensure overflows are handled gracefully */
body, html, #story, #passages, .passage {
    overflow-x: hidden;
}

/* Support for the custom statBlock macro */
.stat-block #strength-value,
.stat-block #dexterity-value,
.stat-block #intelligence-value,
.stat-block #charisma-value {
    font-weight: bold;
    text-align: center;
}

.stat-block #strength-modifier,
.stat-block #dexterity-modifier,
.stat-block #intelligence-modifier,
.stat-block #charisma-modifier {
    font-size: 0.9em;
    color: var(--neon-secondary);
    width: 2em;
    text-align: center;
}

/* ARIA live regions for dynamic content */
[aria-live] {
    border: 0;
    clip: rect(0 0 0 0);
    height: 1px;
    margin: -1px;
    overflow: hidden;
    padding: 0;
    position: absolute;
    width: 1px;
}

[aria-live="polite"] {
    display: block;
    height: auto;
    margin: 0;
    overflow: visible;
    position: static;
    width: auto;
}
